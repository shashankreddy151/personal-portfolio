// config-overrides.js
module.exports = function override(config, env) {
    // Example: You can modify Babel settings here if needed
    // If you just want to suppress the warning, you might not need to change anything
    return config;
  };
  